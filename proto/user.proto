syntax = "proto3"

service UserService{
    rpc addUser(User) returns (User) {}
    rpc getUsers(Empty) returns (User) {}

}

message Empty {
}

message User {
    string id = 1;
    string name = 2;
    int32 age = 3;
    string address = 4;
}

message Users{
    repeated User users = 1
}

